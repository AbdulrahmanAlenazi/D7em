---
description: Deploy updates to GitHub automatically
---
// turbo-all

## Steps to deploy

1. Stage all changes
```bash
git add .
```

2. Commit with a message
```bash
git commit -m "Update"
```

3. Push to remote
```bash
git push
```
